<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.tp.yogioteur.mapper.RoomMapper">

	
	<select id="checkInRoomList" resultType="RoomDTO">
		SELECT ROOM_NO, RT_NO, ROOM_NAME, ROOM_STATUS, ROOM_CHECKIN, ROOM_CHECKOUT, ROOM_PRICE
		  FROM ROOM
		  <![CDATA[
		 WHERE ROOM_CHECKIN >= TO_CHAR('1900-01-01', 'YYYY-MM-DD')
		   AND ROOM_CHECKOUT <  TO_CHAR('9999-12-31', 'YYYY-MM-DD')
		  ]]>
	</select>
	
</mapper>